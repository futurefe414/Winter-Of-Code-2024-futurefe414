<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SastImg.Client.Views.UserProfileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SastImg.Client.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="Transparent"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Padding="24" MaxWidth="800">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Spacing="16" Margin="0,0,0,24">
                <TextBlock
                    Style="{ThemeResource TitleLargeTextBlockStyle}"
                    Text="User Profile" />
            </StackPanel>

            <!-- Content -->
            <StackPanel Grid.Row="1" Spacing="24">
                <!-- Avatar -->
                <StackPanel Spacing="12">
                    <TextBlock Text="Avatar" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    <PersonPicture
                        x:Name="AvatarImage"
                        Width="120"
                        Height="120"
                        HorizontalAlignment="Left" />
                    <Button Content="Change Avatar" Click="ChangeAvatar_Click" />
                </StackPanel>

                <!-- Username -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Username" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                    <TextBlock Text="{x:Bind ViewModel.Username, Mode=OneWay}" />
                    <Button Content="Change Username" Click="ChangeUsername_Click" />
                </StackPanel>

                <!-- Biography -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Biography" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                    <TextBlock
                        Text="{x:Bind ViewModel.Biography, Mode=OneWay}"
                        TextWrapping="Wrap" />
                    <Button Content="Edit Biography" Click="EditBiography_Click" />
                </StackPanel>

                <!-- Password -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Password" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                    <Button Content="Change Password" Click="ChangePassword_Click" />
                </StackPanel>
            </StackPanel>

            <ProgressRing
                Grid.Row="1"
                IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                Width="48"
                Height="48"
                HorizontalAlignment="Center"
                VerticalAlignment="Center" />
        </Grid>
    </ScrollViewer>
</Page>
